version = "0.0.1"

project( ":dropwizard-zookeeper" ) {
  apply plugin: "maven"

  dependencies {
    compile "org.apache.curator:curator-x-discovery:2.4.2"
    compile "io.dropwizard:dropwizard-core:0.7.0"
    compile "com.google.guava:guava:17.0"
  }
}
